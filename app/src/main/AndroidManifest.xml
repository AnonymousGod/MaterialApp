<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ranjan.materialapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OnSavedInstance"></activity>
        <activity android:name=".video_streaming.VideoStreamActivity" />
        <activity
            android:name=".ScrollViewActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".RoomNetworkPagingActivity"
            android:label="@string/title_activity_room_network_paging" />
        <activity
            android:name=".RoomNetworkActivity"
            android:label="@string/title_activity_room_network" />
        <activity
            android:name=".NetworkPagingActivity"
            android:label="@string/title_activity_paging" />
        <activity
            android:name=".RoomActivity"
            android:label="@string/title_room_database" />
        <activity
            android:name=".WebSocketActivity"
            android:label="@string/title_custom_views" />
        <activity android:name=".CustomViewActivity" />
        <activity
            android:name=".AnimationActivity"
            android:label="@string/animation_activity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>